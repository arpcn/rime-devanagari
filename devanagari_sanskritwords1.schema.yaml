# Rime schema
# encoding: utf-8

schema:
  schema_id: devanagari_sanskritwords1
  name: "devanagari_sanskritwords1"
  description: |
    生成詞庫bin的簡易方案，已与devanagari方案建立關聯，不用勾選

engine:
  translators:
    - table_translator

translator:
  dictionary: devanagari_sanskritwords1
